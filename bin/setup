#!/usr/bin/env bash
set -euo pipefail
IFS=$'\n\t'
set -vx

bundle install
bundle install --gemfile=gemfiles/standalone.gemfile
bundle install --gemfile=gemfiles/rbnacl.gemfile

# Do any other automated setup that you need to do here
